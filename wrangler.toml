# Cloudflare Pages Functions 配置
# 用于绑定 KV 命名空间到 Pages Functions

name = "nav-website"
compatibility_date = "2024-01-01"

# KV 命名空间绑定（Pages Functions 使用）
[[kv_namespaces]]
binding = "NAV_KV"
id = "your_kv_namespace_id"  # 创建 KV 后会显示 ID，需要替换
preview_id = "your_preview_kv_id"
